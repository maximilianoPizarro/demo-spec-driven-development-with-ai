schemaVersion: 2.3.0
metadata:
  name: demo-spec-driven-development-with-ai
attributes:
  controller.devfile.io/storage-type: ephemeral  
components:
  - name: tools
    container:
      image: quay.io/maximilianopizarro/lightspeed-demo:latest
      memoryLimit: 4Gi
      memoryRequest: 2Gi
      cpuRequest: 1000m
      cpuLimit: 4000m
      mountSources: true
      volumeMounts:
        - name: config-volume
          path: /home/user/.config
        - name: vscode-extensions-volume
          path: /home/user/.vscode-server/extensions
      endpoints:
        - exposure: public
          name: coolstore
          protocol: http
          targetPort: 8080
  - name: config-volume 
    volume:
      size: 512Mi
  - name: vscode-extensions-volume
    volume:
      size: 1Gi      
commands:
  - id: copyconfig
    exec:
      component: tools
      commandLine: 'bash -c "cd ${PROJECT_SOURCE} && chmod +x setup-continue-config.sh && ./setup-continue-config.sh"'
  - id: build-coolstore
    exec:
      label: 'Build Coolstore application'
      component: tools
      workingDir: ${PROJECT_SOURCE}/coolstore
      commandLine: 'mvn clean install -DskipTests'
  - id: run-coolstore
    exec:
      label: 'Run Coolstore application'
      component: tools
      workingDir: ${PROJECT_SOURCE}/coolstore
      commandLine: 'mvn quarkus:dev'
events:
  postStart:
    - copyconfig
